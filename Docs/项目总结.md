# 影音智增强管理系统 — 项目总结

---

## 一、项目概况

| 项目 | 内容 |
|------|------|
| 框架 | .NET 10 WPF + MVVM (CommunityToolkit.Mvvm) |
| 数据库 | SQLite + EF Core，6张表，全部外键级联删除 |
| 依赖注入 | Microsoft.Extensions.DependencyInjection |
| NuGet 包 | CommunityToolkit.Mvvm、EF Core SQLite、TagLibSharp、Xabe.FFmpeg |
| 界面尺寸 | 1200×750，现代化扁平设计，主色 #2563EB |

---

## 二、项目结构

```
MediaEnhancer/
│
├── App.xaml / App.xaml.cs              # 应用入口、DI容器
├── MainWindow.xaml                     # 主窗口（6页面 + 浮层详情面板）
├── MainWindow.xaml.cs                  # 事件处理
│
├── Models/                             # 6个数据实体
│   ├── MediaFile.cs                    # 媒体文件
│   ├── Thumbnail.cs                    # 缩略图记录
│   ├── Favorite.cs                     # 收藏记录
│   ├── PlayHistory.cs                  # 播放记录
│   ├── EnhancementLog.cs               # 增强日志
│   └── Recording.cs                    # 录屏记录
│
├── ViewModels/
│   └── MainViewModel.cs                # 主视图模型（~1100行）
│
├── Services/                           # 9个服务
│   ├── IDataService.cs / DataService.cs           # 数据服务
│   ├── IFileScanService.cs / FileScanService.cs   # 文件扫描
│   ├── IPlaybackService.cs / PlaybackService.cs   # 播放服务
│   ├── IThumbnailService.cs / ThumbnailService.cs # 缩略图服务
│   └── IAiEnhancementService.cs                   # AI增强预留
│
├── Data/
│   └── AppDbContext.cs                 # 数据库上下文
│
├── Core/                               # 核心算法与工具
│   ├── IEnhancementMethod.cs           # 增强算法接口
│   ├── INativeEnhancement.cs           # C#原生算法接口
│   ├── IOnnxEnhancement.cs             # ONNX接口（预留）
│   ├── EnhancementParameter.cs         # 参数模型
│   ├── LinearStretchMethod.cs          # 线性拉伸算法
│   ├── MediaFileUtils.cs               # 媒体文件工具类
│   └── *Converter.cs                   # 值转换器
│
├── Views/                              # 辅助窗口
│   ├── InputDialog.xaml/.cs            # 文本输入对话框
│   ├── DeleteConfirmDialog.xaml/.cs    # 删除确认对话框
│   ├── MediaPlayerWindow.xaml/.cs      # 媒体播放器
│   └── ImageViewerWindow.xaml/.cs      # 图片查看器
│
└── Docs/
    └── 项目总结.md                     # 本文件
```

---

## 三、页面导航

| 索引 | 页面 | 状态 | 核心功能 |
|:----:|------|:----:|---------|
| **0** | 📊 **数据统计** | ✅ | 8卡仪表盘、快捷入口、最近播放、检查依赖、清理缓存 |
| **1** | 📂 **文件管理** | ✅ | 搜索筛选、扫描导入、收藏/重命名/删除、浮层详情、批量操作 |
| **2** | ✨ **实时增强** | ✅ | 线性拉伸算法、参数调节、预览导出 |
| 3 | ⏺ 屏幕录制 | ⏳ | 界面占位 |
| 4 | 🤖 AI 总结 | ⏳ | 界面占位 |
| 5 | ⚙ 系统设置 | ⏳ | AI设置、保存路径配置 |

---

## 四、已实现功能

### 4.1 文件管理

| 功能 | 说明 |
|------|------|
| 文件夹批量扫描 | 递归遍历 → TagLibSharp 提取元数据 → 批量入库 |
| 单/多文件导入 | 文件选择器 → 自动去重 → 入库 → 自动缩略图 |
| 搜索筛选 | 关键词 + 类型（图片/视频/音频）+ 收藏 三维组合 |
| 收藏切换 | DataGrid 勾选框实时切换 |
| 文件重命名 | 输入对话框 → File.Move → 同步数据库 |
| 文件删除 | 三级确认：取消 / 仅删记录 / 删源文件+记录 |
| 浮层详情面板 | 双击行展开，VS Code 风格右侧浮层 |
| 文件校验 | 遍历记录 → 缺失时提供删除/定位 |

### 4.2 播放功能

| 类型 | 播放器 | 功能 |
|:----:|--------|------|
| 🖼 图片 | ImageViewerWindow | 缩放、拖拽平移、上一张/下一张 |
| 🎬 视频 | MediaPlayerWindow | 播放/暂停、进度条、音量、全屏、快捷键、上下首 |
| 🎵 音频 | MediaPlayerWindow | 同上，音频模式占位封面 |

### 4.3 缩略图服务

| 文件类型 | 生成方式 |
|---------|---------|
| 图片 | 缩放到 200px → JPEG 缓存 |
| 视频 | ffmpeg 提取第一帧（<2s）或第 2 秒帧 → JPEG 缓存 |
| 音频 | 生成默认图标（🎵 绿色背景）→ JPEG 缓存 |

缓存目录：`运行目录/Thumbnails/`，可在系统设置中修改。

### 4.4 增强模块

```
IEnhancementMethod（统一接口）
  ├── INativeEnhancement（C#原生，实时可用）
  │     └── LinearStretchMethod ← 已实现
  ├── IOnnxEnhancement（ONNX模型，预留）
  └── IAiEnhancementService（大模型API，预留）
```

| 功能 | 说明 |
|------|------|
| 线性拉伸算法 | WriteableBitmap 像素操作，<0.1ms |
| 参数调节 | 对比度强度（0.5~2.0）+ 亮度偏移（-50~50） |
| 预览 | 选择图片 → 增强 → 实时预览 |
| 导出 | 保存到 Enhancements/ 目录 → 自动入库 |
| 文件中增强 | 详情面板「✨ 增强」按钮 |

### 4.5 批量操作

工具栏底部固定显示，选中 ≥2 个文件后按钮启用：

| 按钮 | 功能 |
|:----:|------|
| ⭐ 收藏 | 切换所有选中文件收藏状态 |
| 🗑 删除 | 确认后批量删除 |
| 🖼 缩略图 | 批量生成缩略图 |
| ✨ 增强 | 批量线性拉伸 → 入库 |

### 4.6 数据统计仪表盘

| 卡片 | 数据来源 |
|:----:|---------|
| 📁 文件总数 / 🖼 图片 / 🎬 视频 | MediaFiles 表统计 |
| ✨ 增强次数 / ⏺ 录屏次数 | EnhancementLogs / Recordings |
| ▶ 总播放次数 / ⭐ 收藏数 | PlayHistories / MediaFiles |
| 📦 检查依赖 | 检测 ffmpeg.exe 并自动下载 |
| 🗑 清理缓存 | 清空缩略图缓存 |

### 4.7 数据库设计

| 表 | 关键字段 | 关联 |
|:--|---------|:----:|
| **MediaFiles** | 14字段（含 IsFavorite / Description） | — |
| **Thumbnails** | FilePath, CreatedAt, LastAccessAt | → MediaFile |
| **Favorites** | CreatedAt | → MediaFile |
| **PlayHistories** | PlayedAt, PlayProgress | → MediaFile |
| **EnhancementLogs** | MethodName, OutputPath | → MediaFile |
| **Recordings** | Duration, IsEnhanced, AudioSource | → MediaFile |

所有外键表均配置 `OnDelete(DeleteBehavior.Cascade)`。

---

## 五、文件格式支持

| 类别 | 格式 |
|:----:|------|
| 🖼 图片 | `.jpg .jpeg .png .bmp .gif .webp` |
| 🎬 视频 | `.mp4 .avi .mkv .mov .wmv .flv .webm` |
| 🎵 音频 | `.mp3 .wav .flac .aac .ogg .wma .m4a` |

---

## 六、待实现功能

| 优先级 | 模块 | 内容 |
|:------:|------|------|
| 🥇 | **实时屏幕增强** | 透明覆盖窗口 + DXGI 屏幕捕获 + 实时线性拉伸 |
| 🥇 | **离线视频增强** | 逐帧提取 → 增强 → 重新编码 |
| 🥇 | **增强图缩略图** | 增强生成的新图片自动生成缩略图 |
| 🥈 | **录屏服务** | 屏幕录制 + 硬件编码 |
| 🥈 | **AI 总结 / 模板化问答** | |
| 🥉 | **课程文档** | 安装说明、使用说明、课程设计报告 |

---

## 七、已知问题

| 问题 | 状态 |
|------|:----:|
| 视频封面帧提取依赖 ffmpeg | 需手动点击「检查依赖」下载 |
| 中文文件路径的兼容性 | 已修复（ArgumentList + 临时文件） |
| 增强后图片的缩略图生成 | 待解决 |
| DataGrid 收藏勾选框交互 | 待验证 |
