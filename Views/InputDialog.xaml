<Window x:Class="MediaEnhancer.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="输入" Height="180" Width="420"
        WindowStartupLocation="CenterOwner"
        WindowStyle="SingleBorderWindow"
        ResizeMode="NoResize"
        FontFamily="Microsoft YaHei"
        Background="#F8FAFC">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="PromptText"
                   Text="请输入新名称："
                   FontSize="14"
                   Foreground="#0F172A"
                   Margin="0,0,0,12"/>

        <TextBox Grid.Row="1" x:Name="InputBox"
                 FontSize="14"
                 Padding="8,6"
                 BorderBrush="#CBD5E1"
                 BorderThickness="1"
                 Margin="0,0,0,16"
                 KeyDown="InputBox_KeyDown"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="取消"
                    Width="80" Height="32"
                    Background="White"
                    Foreground="#475569"
                    BorderBrush="#CBD5E1"
                    BorderThickness="1"
                    Cursor="Hand"
                    Margin="0,0,8,0"
                    Click="Cancel_Click"/>
            <Button Content="确定"
                    Width="80" Height="32"
                    Background="#2563EB"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand"
                    Click="OK_Click"/>
        </StackPanel>
    </Grid>
</Window>
